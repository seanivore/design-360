# Tile Design Update 

## Mobile Optimization 

  + This is our inspiration for the following reasons: `/Users/seanivore/Development/360-design/assets/docs/tile_visual_inspo/example.PNG`
    - Large images 
    - Image bleed
    - Tile takes up screen for 1 at a time 
    - Text below 

  + This is our current homepage tile: `/Users/seanivore/Development/360-design/assets/docs/tile_visual_inspo/current-home.PNG`

    2. Give these three links smaller gap and align them at the bottom of the container 
    1. Then Sean August Horvath will have a bit of breathing room 
    3. We should really get as wide as we can just like in the Google Screenshot; closer than the text there 
    4. Give these dots less space and make them smaller -- they are secondary and only really tell users to swipe 
    5. Similarly to the width of the whole tile, we don't want this much padding for the text container either 
    6. Then we need to make sure the rest of the text is not falling off the tile, but we should be good with all the other adjustments 

  + This is our current section page tile, image 1: `/Users/seanivore/Development/360-design/assets/docs/tile_visual_inspo/current-section-1.PNG`
    and an uncluttered version, image 2: `/Users/seanivore/Development/360-design/assets/docs/tile_visual_inspo/current-section-2.PNG` 

   - Hopefully you can already see the issues! 
   - The image doesn't fit in the tile 
   - The text is all squished 
   - This is particularly awkward because we have SO MUCH SPACE 

   1. Same header updates here as homepage and across entire site 
   2. For the filter nav, the background of it needs to differentiate from the site background, then we can have it literally bleed because of the UI
   3. At the same time I think we can push these tags closer together, maybe even bigger a bit, and have the first one nicely aligned left 
   4. For these section page tiles, I think we should try an actual bleed the tile width because we're going to make it more 1-tile per screen at a time 
   5. then the images can be padded down a bit to show there is a top boundary to the tile 
   6. I'm not sure if we want to keep the previous and next image, or if we want to try something like in the example image 
   7. Then we have room for the text below; the sketch is a bit bigger than we'd need so it can be smaller for sure 



